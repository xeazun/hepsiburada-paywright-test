{
  "name": "hepsiburada-web-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
